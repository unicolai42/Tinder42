{
  "name": "test_crae",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "(cd client && npm install) && (cd server && npm install)",
    "start": "concurrently \"cd client && PORT=3000 npm start\" \"cd server && PORT=3001 npm start\"",
    "restart": "(cd client && npm install) && (cd server && rm -rf node_modules/bcrypt && npm install)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.0",
    "concurrently": "^3.6.0",
    "es6-promise": "^4.2.5",
    "fb": "^2.0.0",
    "isomorphic-fetch": "^2.2.1",
    "mysql": "^2.16.0",
    "passport": "^0.4.0",
    "randomstring": "^1.1.5",
    "react-geocode": "^0.1.2",
    "request": "^2.88.0",
    "request-promise": "^4.2.2",
    "unsplash-js": "^4.8.0"
  }
}
